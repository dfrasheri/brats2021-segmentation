# Pinned to what this project actually imports. Versions are the ones the
# reported results were produced with.

# Deep learning. The +cu121 build is what was used; install the CUDA build that
# matches your driver from https://pytorch.org for GPU training.
torch==2.5.1

# Medical imaging I/O
nibabel==5.3.3

# Numerics. scipy provides binary_erosion (surface extraction) and cKDTree (HD95).
numpy==2.3.5
scipy>=1.10.0
pandas>=1.5.0

# Data download (notebook only)
kaggle>=1.7.0

# Notebooks and figures
matplotlib==3.10.8
jupyter>=1.0.0
ipywidgets>=8.0.0
tqdm==4.67.1
